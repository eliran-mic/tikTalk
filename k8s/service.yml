apiVersion: v1
kind: Service
metadata:
  name: tiktalk
  labels:
    app: tiktalk
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: tiktalk
